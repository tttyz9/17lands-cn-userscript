# 更新日志

### v0.43（最新）
- 修复「卡组构筑器无法拖拽卡牌」：v0.41 引入的拖拽兜底保护里，document 在【捕获阶段】监听全局 drop 并立即清空 DRAG，而容器的落点处理(handleDrop)是【冒泡阶段】监听，捕获阶段先执行导致容器处理时 DRAG 已为 null、松手落下被跳过（表现为拖得动、有插入线、但松手无效）。现已删掉 drop 的捕获清空监听，仅保留 dragend + window blur 两处兜底，既修复落点、又不会让 DRAG 卡死。

### v0.42（最新）
- 修复「第二次打开构筑器（含切换 Other Seat）后卡图悬浮放大失效」：根因为 buildUI 删除悬浮浮层后未清空缓存变量，现已在节点脱离文档时自动重建挂回。

### v0.41
- 修复卡图悬浮放大消失问题（根因在 v0.42 彻底修正）。

### v0.40
- 支持打开 Other Seats（轮抓历史中 `?seat=1..7`）的卡池。
- 自动配置地卡改为按颜色符号数量加权、低费咒语优先级更高，配地更合理。
- 打印卡图时指示物不再带金框与角标。

### v0.39
- 修复手动加地 / 自动配地加入的基本地未计入「地」张数统计的 bug。
- 构筑器内所有 label 间距统一对齐。

### v0.38
- 顶栏控件去除多余下边距、严格对齐；清理冗余 CSS。
- （注：本次对卡图缩放基准的放大改动实际未生效，文件仍维持原尺寸。）

### v0.37
- 顶栏三类控件（按钮 / 缩放滑块 / 含指示物）彻底统一为等高、同圆角、同背景。
- token 数量弹窗的确认/取消按钮回退为内联样式。
- 打印进度只统计主牌卡图，指示物绝不空白。

### v0.36
- 修复打印页指示物空白的问题；顶栏三控件真正等高对齐。

### v0.35
- 构筑器卡图悬浮与 token 弹窗悬浮合并为单一共享浮层，去掉金框。
- 顶栏三类控件统一为 32px 等高。

### v0.34
- 修正法术力曲线「下方费用刻度标签」(0/1/2…7+) 与柱体的间距（此前误改了上方张数）。
- token 弹窗小图 hover 跟随鼠标放大。
- 顶栏普通按钮 / 缩放滑块 / 复选框改为三类独立样式；筛选栏复选框垂直对齐。

### v0.33
- 修复 token 数量弹窗中卡图破裂：弹窗显示前预转为内嵌 PNG。
- 顶栏控件严格统一 28px 高度。
- 加大曲线柱子上方数字与柱体的间距。

### v0.32
- 顶栏按钮统一高度 / 圆角 / 阴影。
- 按钮改名：「导出TTS牌表」→「导出牌表」、「导出Word卡图」→「打印卡图」。
- 地卡统计修正（含非基本地计入“地”）；自动配地改为净增减避免重复计算。
- 打印弹窗可自选每个指示物的打印数量。
- 移除废弃的 html-docx-js 依赖。

### v0.31
- 打印卡图时自动附印关联指示物（Token）：抓取主/备牌非地卡的 token 并分页打印。
- 顶栏新增「含指示物」开关（默认开）。
- 修复 webp/jpg 转换；Scryfall 请求加限流防 429。
